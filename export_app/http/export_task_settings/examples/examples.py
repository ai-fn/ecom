id = 1
name = "Example Name"
fields = {
    "product": ["id", "title", "category"]
}

example_export_settings_successful_response = {
    "id": id,
    "name": name,
    "fields": fields
}

example_export_settings_create_request = {
    "name": name,
    "fields": fields
}

example_export_settings_update_request = {
    "name": name,
    "fields": fields
}

example_export_settings_partial_update_request = {
    "name": name,
}
